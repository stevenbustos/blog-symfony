{% extends 'base.html.twig' %}

{% block title %}
    {{ post.title }}
{% endblock %}

{% block body %}
    <div class="jumbotron">
        <h1 class="display-4">{{ post.title }}</h1>
        <p class="lead">
            {{ post.author }}
            <br>
            {{ post.createdAt|date("m/d/Y") }}
        </p>
        <hr class="my-4">
        <p>
        <div class="row">
            <div class="col">
                {{ post.content }}
            </div>
            <div class="col">
                <img src="{{ asset('uploads/images/') ~ post.image }}" width="300" height="300" class="d-inline-block align-top" alt="">
            </div>
        </div>
        </p>
    </div>
{% endblock %}